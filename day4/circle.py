import math
def perfectSquare (radius):
    for a in range(1,radius*2):
        text = ""
        for b in range(1, int(round(radius*2))):
            if :
                text += " "
            else:
                text += "O"
        print text

def hypotenuse (a, b):
    return s
perfectSquare(6)
